[package]
name = "hello-voice1"
version = "0.1.0"
description = "Local OSS voice assistant"
authors = ["hello-voice1"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[features]
custom-protocol = ["tauri/custom-protocol"]
